# Server Configuration
PORT=5000
NODE_ENV=development

# Database Configuration (PostgreSQL)
# Railway Best Practice: Use DATABASE_URL only
# For Railway Deployment: Use ${RAILWAY_PRIVATE_DOMAIN} (Railway will replace this)
# DATABASE_URL=postgresql://postgres:password@${RAILWAY_PRIVATE_DOMAIN}:5432/railway
#
# For Local Development with Railway TCP Proxy:
# DATABASE_URL=postgresql://postgres:password@containers-us-west-xxx.railway.app:6543/railway
#
# For Local PostgreSQL:
# DATABASE_URL=postgresql://postgres:password@localhost:5432/namma_naidu
#
# Note: Railway auto-injects PGUSER, PGPASSWORD, PGDATABASE, PGHOST, PGPORT
# DO NOT manually define those variables - Railway handles them automatically

# JWT Configuration
JWT_SECRET=your-secret-key-here
JWT_EXPIRE=2d

# CORS Configuration
CORS_ORIGIN=http://localhost:3000

# OTP Configuration
OTP_STATIC_CODE=12345
OTP_EXPIRY_MINUTES=10

# API Logging Configuration
# Enable file logging (logs will be saved to logs/ directory)
# Set to 'true' to enable file logging, 'false' or leave empty to disable
ENABLE_FILE_LOGGING=false
# Enable debug logging (shows detailed debug information)
DEBUG=false

# Cloudinary Configuration
# Option 1: Use CLOUDINARY_URL (recommended)
CLOUDINARY_URL=cloudinary://api_key:api_secret@cloud_name

# Option 2: Use individual config variables
# CLOUDINARY_CLOUD_NAME=your_cloud_name
# CLOUDINARY_API_KEY=your_api_key
# CLOUDINARY_API_SECRET=your_api_secret

# Firebase Admin SDK Configuration
# REQUIRED for Firebase OTP login and push notifications
#
# Option 1: Railway/Production (RECOMMENDED)
# 1. Go to Firebase Console → Project Settings → Service Accounts
# 2. Click "Generate new private key" and download the JSON file
# 3. In Railway Dashboard → Your Service → Variables:
#    - Key: FIREBASE_SERVICE_ACCOUNT
#    - Value: Paste the ENTIRE JSON content (can be multiline, Railway handles it)
# 4. Restart your Railway service
#
# Example format (Railway accepts multiline JSON):
# FIREBASE_SERVICE_ACCOUNT={
#   "type": "service_account",
#   "project_id": "your-project-id",
#   "private_key": "-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----\n",
#   "client_email": "firebase-adminsdk@your-project.iam.gserviceaccount.com",
#   ...
# }
#
# Option 2: Local Development
# 1. Download firebase-service-account.json from Firebase Console
# 2. Place it in: src/config/firebase-service-account.json
# 3. The app will automatically load it if FIREBASE_SERVICE_ACCOUNT is not set

# Email Configuration (Nodemailer)
# REQUIRED for sending OTP via email
# For Gmail:
# 1. Go to your Google Account settings
# 2. Enable 2-Step Verification
# 3. Generate an App Password: https://myaccount.google.com/apppasswords
# 4. Use the app password as EMAIL_PASS
EMAIL_USER=your-email@gmail.com
EMAIL_PASS=your-app-password

# Alternative: Use other email services (SMTP)
# Uncomment and configure if not using Gmail:
# EMAIL_HOST=smtp.gmail.com
# EMAIL_PORT=587
# EMAIL_SECURE=false


